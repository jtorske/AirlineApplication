package Domains.GUI;

public class AdminLogin extends Login{
    public AdminLogin(){
        createView();
        setTitle("Login As A System Admin");
        setSize(400, 200);
        setLocationRelativeTo(null);
        setResizable(false);
    }
}
